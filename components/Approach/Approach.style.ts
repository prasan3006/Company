import { css } from "emotion";

export const ApproachStyle = () => css`
  .tab-right {
    position: relative;
    img {
      width: 20px;
      height: 20px;
      position: relative;
      left: -1px;
      top: 11px;
    }
  }
`;
