import { css } from "emotion";

const FooterStyle = () => css`
  .copyright-area {
    .section-wrapper {
      display: flex;
      flex-direction: row;
      justify-content: space-between;
    }
  }
`;

export default FooterStyle;
